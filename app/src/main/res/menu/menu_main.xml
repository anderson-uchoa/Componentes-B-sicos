<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activity.MainActivity">
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />

    <item android:id="@+id/action_add_nota"
        android:orderInCategory="1" app:showAsAction="never"
        android:title="Adicionar Nota" />

    <item android:id="@+id/action_add_categoria"
        android:orderInCategory="2" app:showAsAction="never"
        android:title="Adicionar Categoria" />

    <item android:id="@+id/action_background"
        android:orderInCategory="3" app:showAsAction="never"
        android:title="Escolher Background" />

</menu>
